anthropic>=0.40.0
pandas>=2.0.0
ccxt>=4.0.0
pyyaml>=6.0
python-dateutil>=2.8.0
aiohttp>=3.9.0
tqdm>=4.66.0
rich>=13.0.0

# optional: only needed for ops_yield --gsheet-url (publish to Google Sheets)
gspread>=6.0
